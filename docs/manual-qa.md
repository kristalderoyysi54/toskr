# 手动验收清单

发布前逐项过一遍（涉及全局键盘、焦点、剪贴板与多屏行为，自动化难以覆盖）。

1. 任意 App 双击 ⇧ ⇄ 面板滑出/收起；出现在光标所在屏
2. 划词双击 ⇧ → 面板不弹、气泡闪现、卡片入库、原剪贴板不破坏
3. 大写打字/长按/组合键不误触发；设置改双击 ⌃ 后 ⇧ 失效 ⌃ 生效
4. 连拍捕获显示 ×N；重复内容提示已存在不重复入库
5. 气泡悬停出现「撤销」，点击后卡片移除；移开恢复穿透
6. iTerm/终端前台双击 ⇧ → 面板吸附其右缘同高；拖动/缩放窗口实时跟随；切到 Safari 回退屏幕右缘
7. 「把当前应用加入伴随列表」生效；终端半屏靠右时面板左收钳制屏内
8. ⌘F 搜索命中高亮/计数/清空；Esc 分层退出
9. ↑↓/Space/Enter/⌘⌫/⌘A 全键盘操作；拖拽把手 Space 键盘排序
10. 删除/合并/清理已完成 → toast 撤销恢复原位
11. 已完成沉底折叠展开；顶栏清理按钮
12. 跨组拖拽；拖到折叠组悬停自动展开
13. 当前发送方案的“粘贴后动作”为“仅粘贴”：从 TextEdit/Codex 打开 Toskr，勾选 synthetic 文本发送 → 只粘贴、不回车；成功 HUD 的应用名与实际目标一致，卡片才标完成
14. 发送前退出目标 App → `blocked/target_exited` 气泡；无粘贴、无回车，卡片未完成、选择与面板恢复
15. Pin 面板 → 切 App B → 回面板发送 → 落点只允许为 B；切换后旧 token 不得继续发送到 A
16. 仅在一次性 TextEdit 文档中把精确发送方案的“粘贴后动作”设为“粘贴并回车”：文本粘贴后、200ms 回车窗口内抢焦点到 App B → B 无粘贴且两边都无回车；结果为 focus drift，卡片与选择保留
17. iTerm/终端作为目标且保持默认设置 → 只粘贴、不自动回车；确认设置默认值仍为关闭
18. 全屏 Safari/终端上双击 ⇧ 面板与气泡均可见
19. 隐身模式开启：捕获入库但无气泡
20. 宽度拖拽调节并重启记忆
21. 导出完整备份 → 删卡 → 预检并恢复；发送按钮悬停预览与实际粘贴一致
22. 三步上手引导逐项打勾并庆祝；重启数据完整；单实例聚焦

## 剪贴板事务（Phase 02）

23. TextEdit 新建含粗体、颜色与链接的富文本并复制 → 向另一应用发送一条 Toskr 文本 → HUD 显示原剪贴板已恢复 → 回到空白 TextEdit 粘贴，文字与 RTF/HTML 样式保持一致
24. Finder 同时复制两个测试文件 → 完成一次 Toskr 发送 → 在临时空目录粘贴，两个文件均可复制且顺序/类型未退化为纯文本
25. Preview 复制一张图片 → 完成一次发送或复制回退捕获 → 使用“从剪贴板新建”，图片内容与尺寸仍一致
26. 分三次发送，在 Toskr 写入后约 100ms、500ms、1400ms 复制新的 synthetic 文本 → 每次最终剪贴板均保留新文本，HUD 显示“已保留你刚复制的内容”
27. 先复制 sentinel，再在目标应用取消选区并触发捕获 → 不新增历史 sentinel 卡片；原剪贴板仍可粘贴 sentinel；另在可延迟复制超过 500ms 的应用复测，迟到内容不得入库且宽限窗内原 sentinel 被恢复
28. 在实际偏慢的聊天应用发送文本/图片 → 内容成功落入目标；约 1.5s 后原剪贴板恢复，期间手动复制的新内容不被覆盖

## 数据事务、完整备份与媒体完整性（Phase 02A）

29. 设置 → 数据 → 预检一个临时空目录：确认只显示“迁移当前数据 / 取消”；迁移后主窗口立即展示原记录与图片，活动目录更新，重启后内容与媒体仍一致
30. 预检另一个含有效 Toskr 数据的临时目录：记录/任务/媒体数量可见；分别验证“加载目标”“取消”和二次确认后的“创建恢复点后替换目标”，两个有效数据集不出现自动 merge
31. 预检默认目录：默认目录为空时显示迁移语义；已有有效数据时显示加载/显式替换语义；完成后主窗口与 Settings 显示同一活动目录，重启不出现旧快照
32. 在复制、验证、指针提交或重新水合期间制造失败（测试包/故障注入）：原目录指针、原内存与目标已有数据均恢复；目标不被半成品识别为活动目录
33. 应用打开期间用外部编辑器改动 `toskr-data.json`，再在 Toskr 编辑：旧内存不得覆盖磁盘；设置页出现“重新加载 / 另存恢复副本后加载 / 暂不处理”，三条路径均不静默采用最后写入者
34. 导出 `.toskr-backup`，在全新临时目录导入：notes、tasks、sections、taskSections、允许备份的设置、图片数量与内容 hash 一致；归档中搜索不到 AI API Key，导入确认也明确提示需重新配置
35. 导入旧 `.json`：预检明确提示可能缺媒体与 taskSections；创建当前数据恢复点后才合并；导入结束运行健康检查，不把旧 JSON 描述为完整备份
36. 清理含图片的剪贴板历史后立即撤销：图片仍可显示；等待撤销/GC 宽限结束并重启，仍被其他 Note/Task 引用的同名图片不得删除
37. 数据健康检查构造缺失、孤立、共享和撤销栈引用媒体：只显示数量与修复建议，不自动删除缺失记录或无法确认的孤立文件
38. 数据操作“冻结/重新水合”覆盖层可见时尝试双击捕获、剪贴板推送、详情保存、键盘删除和修改设置：所有写入均被阻止，操作完成或回滚后恢复
39. 使用故障注入包分别在 recovery/staging 持久化、旧目标搬移、指针提交、前端 rehydrate、rollback recovery copy 后强制退出：重启只恢复或继续同一 operation ID；外部新版本不被删除，journal 未完成前普通写入仍禁止
40. 将自定义数据目录暂时离线或让默认数据 JSON 损坏后启动：应用进入 recovery-only 界面而不是静默回默认/直接退出；“重试挂载”与“选择并加载另一个有效目录”可用，普通编辑仍只读
41. 备份或目录预检显示 A 后，在确认窗期间用测试进程原地替换成另一份同 schema 的 B：load、replace、migrate、完整恢复与旧 JSON 合并均以 `externalConflict` 停止，不按 A 的计数宣称处理了 B
42. 在数据冲突后另存恢复副本：若当前媒体仍匹配内存记录的内容寻址文件名则导出成功；若同名 PNG 已被外部替换，必须拒绝生成“记录 A + 图片 B”的伪完整备份
43. 对两张到期墓碑执行 GC，并分别在 journal 落盘、文件隔离、隔离文件删除后强制退出：重启能恢复或完成；重复排期不能把墓碑重绑定到同名外部新文件，后一文件冲突时前一文件也回放
44. Native 数据事务已经提交指针、等待前端 rehydrate 时单独重载 main WebView：启动协调器在开放写入前续接或回滚 pending operation；完成后 hydration、启动 GC 与运行时设置只执行最终目录版本
45. 在目标数据的快捷键/剪贴板监听/主题等运行时设置下发过程中注入失败：native finalize 不早于全部设置命令；回滚后只保留旧目录设置，界面解除只读前不出现两批 IPC 交错
46. 外部冲突发生后重载或晚打开 Settings：冲突状态仍持久可见，“重新加载 / 另存恢复副本后加载 / 暂不处理”可聚焦；普通设置 patch 仍被 main 权威写闸拒绝

## Target Lens 发送目标可视化（Phase 03，RUNTIME-REQUIRED）

47. 分别从 TextEdit 与 Codex 打开 Toskr：Target Lens 第一层按“小图标 → 应用名 → 分隔点 → 状态点与可发送”单行紧凑展示，状态无背景胶囊；第二层解释 exact/fallback/temporary/conflict 来源；第三层显示提示词组、输出格式、粘贴后动作和“隐私检查尚未启用”；发送落点、成功 HUD 应用名与 Lens 一致。临时方案暂停时必须显示“需确认”，不得误报“可发送”
48. Pin 面板后从 App A 切到 App B：Lens 只更新为 B，不清空当前选择、草稿或预览；发送只能落到 B，不能回投 A
49. 伴随/磁吸仍指向 App A 时切到 App B：Lens 反映 B 而不是停靠对象；开关伴随和停靠方向不改变发送目标语义
50. 在独立窄面板、右/左侧边栏、上/下横向条分别检查 ready / refreshing / blocked：目标条不横向滚动、不变成厚重工具栏，长应用名可截断，阻断原因仍可见
51. 目标 App 退出或 PID/launch identity 变化且 Toskr 仍在前台：Lens 自动转为阻断；笔记、任务、批量、预览和快捷键入口均不粘贴、不回车，HUD 与 VoiceOver 给出同一原因
52. 快速 A→B→A 切换并连续点“重新识别”：旧异步响应不得回滚新目标，图标不闪回、相同快照不重复请求图标或制造事件风暴
53. 使用缺少图标的测试 App：显示稳定 fallback；键盘可聚焦并触发“重新识别”；VoiceOver 精确读出目标名、状态、发送方案、提示词组、输出格式、粘贴后动作，并明确播报“隐私检查尚未启用”；目标变化只由克制的 aria-live 播报一次，不重复整块内容
54. 在一次性 TextEdit 文档分别配置发送方案的“仅粘贴 / 粘贴后询问是否回车 / 粘贴并回车”：Lens 即时更新，实际发送分别只粘贴 / 确认后回车 / 直接回车；焦点漂移时仍由 Native gate 阻断

## 发送方案与提示词组（Phase 04，RUNTIME-REQUIRED）

55. 设置分为“目标与模板”（发送方案 + 提示词组/模板）与独立的“伴随停靠”分区；匹配预演输入 bundle identifier 后显示“自动匹配：<方案名称>”或冲突；两区均键盘可达，深链 snippets → 目标与模板、companion → 伴随停靠
56. 用 v8 临时数据启动新包：旧提示词的非冲突 ID、名称、正文和顺序全部保留并归入“通用”；重复/空 ID 稳定重编号但不丢模板；旧 autoEnter=true 只迁为默认发送方案的“粘贴后询问是否回车”
57. 分别为 Codex、Claude、浏览器配置适用应用，再逐个切换：Lens 的发送方案、提示词组、输出格式和粘贴后动作随真实 bundle 命中，并解释“已为 <应用> 指定”；未配置应用解释“未匹配具体应用，使用默认方案”；无论隐私预设为何，主面板与快速切换浮层都显示“隐私检查尚未启用”
58. 逐个切换仓库既有终端 bundle：默认“终端（安全）”发送方案命中，粘贴后动作始终为“仅粘贴”；应用未安装的 bundle 留在配置中也不导致设置页崩溃
59. 尝试把已绑定的 synthetic bundle 新增到另一发送方案：设置页阻止新增且保留其他合法编辑；再用历史重复数据启动，设置页与 Lens 以黄色标记 conflict、按列表首项稳定命中，点击“保留在 <方案>”后只移除其他方案的该绑定
60. 在 App A 打开快速切换浮层，选择另一方案后只产生绑定 A 快照的临时覆盖，永久适用应用保持不变；只有再点“以后发给 A 都使用此方案”才迁移永久绑定。浮层保持打开时切到 App B：标题、图标、原因与规则立即更新，旧覆盖清除，B 使用自己的 exact/fallback；B 失效时全部方案选择与发送入口锁定
61. 删除正被 snippet 和多个发送方案引用的提示词组：全部引用回落“通用”，HUD 报告修复数量，重启后无孤儿引用
62. 在 380pt 竖面板与横向条用长应用名、长方案名、VoiceOver/Tab 检查 Lens 与快速切换浮层：Enter/Space 打开，↑↓ 循环移动，Enter 临时选择，Escape 关闭；当前摘要、最多三项方案、恢复自动、编辑入口均可达；不得横向溢出，状态标签自然换行且最多两行

## 签名与 TCC 备注

构建使用本机自签名证书 **"Toskr Dev Signing"**（`bundle.macOS.signingIdentity`），签名跨编译稳定，重新编译后无需重新授权辅助功能。换机器时：先在钥匙串创建同名代码签名证书并信任，或移除该配置退回 ad-hoc（每次重编译需在系统设置里删掉 Toskr 条目重新添加）。`pnpm tauri dev` 的调试进程权限附着于启动它的终端。

## AI 智能（v0.9.x 新增）
63. 设置 → AI 智能：预设点选填 Base URL（模型名为空才带入推荐值）；API Key 只显示“已配置/未配置”，新值只可保存或覆盖且不能回显；「测试连接」从 macOS 钥匙串读取 key，未启用 AI 总开关也能执行
64. 任务输入框 ✨ 模式：「下午3点提醒我开会」回车 → 生成带到期时间的任务；AI 未配置/断网时按原文创建普通任务（输入不丢失）+ warn 气泡；💡 与 ✨ 互斥
65. 任务右键「AI 拆解子任务」→ 写入 3-8 条检查项，HUD 可撤销；笔记右键「AI 转任务」→ 建任务删笔记一次撤销恢复；「AI 起标题」→ 卡片通栏出现短标题

## DeliveryDraft 单一发送管线（Phase 05，RUNTIME-REQUIRED）

66. 在一次性 TextEdit 文档中，用单卡、批量、`⌘1…9`、`⌘Enter` 与提示词模板发送同一 synthetic fixture；逐字节比较 Tooltip 预览与实际粘贴，换行、编号、Prompt 占位符和特殊字符均一致
67. 分别发送链接、单条带语言代码、多条混合代码、带真实说明的图片和多图组合：单条代码保留语言，多条代码块不带语言；图片说明进入正文，尺寸占位不进入正文，附件按卡片列表首次出现顺序且不重复
68. 发送包含标题、备注与 checklist 的 synthetic 任务：目标收到稳定 Markdown，任务状态不因发送改变；blocked/failed 时面板恢复且原选择不丢失
69. 分别使用“仅粘贴 / 询问后回车 / 粘贴并回车”的发送方案：取消确认时无粘贴，确认后 Native 收到的回车决策与 Draft 一致，Pin 只改变发送后面板是否保留
70. 发送确认、目标刷新或 Native 回执在途时，依次修改来源正文、新增勾选、切换目标与更改发送方案：旧 Draft 不得修改 done、checked 或 onboarding，新勾选必须保留并显示明确警告
71. 快速双击同一发送入口：只允许一个 Native 发送在途；第二次显示“已有发送正在进行”，首个成功回执仍正常标完成并清除其原始选择
72. 发送前退出目标、发送中切走焦点及模拟不可读图片：Native 必须在任何粘贴/回车副作用前阻断；所有入口统一恢复面板、保留卡片与选择

## 发送预检台（Phase 06，RUNTIME-REQUIRED）

73. 普通竖向面板分别发送单条稳定纯文本、多条、图片、Prompt、代码块及 allow/confirm 回车方案：smart 只让首项快速发送，其余进入同一预检；always 全部进入，off 保持快速发送；菜单“预检并发送”始终显式打开
74. 在预检中逐项核对目标图标/名称、发送方案、来源展开列表、图片缩略图、Prompt、输出格式、真实最终正文、回车决定、保留面板和 warning；修改最终正文后原 Note/Task 不变，“恢复自动组装内容”还原本次 Draft
75. 打开预检后分别编辑/删除/移组来源，新增勾选，切换目标、临时方案或数据目录：确认按钮立即禁用并播报对应 stale 原因；切换 Prompt/格式不得偷换来源基线
76. Pin 与非 Pin 各发送一次；再在伴随模式下从 App A 切 App B 后打开预检：目标只显示 B，keepPanel 只反映本次决定；A 的迟到回执不得清除 B 新选的临时方案
77. 分别制造目标刷新失败、Native 首次 paste 前 blocked、paste 后焦点漂移、Enter 失败和 IPC 回执丢失：零粘贴失败保留可编辑重试；同目标安全回执只更新 token；可能已粘贴的结果锁定原 Draft 并要求先核对，不能重复外发
78. 在 380pt 竖面板以及 260–420pt 上/下横栏打开完整发送菜单和预检：菜单顶部“预检并发送/预检方式”可见且可滚动，预检竖向分区或横向双栏均无横向溢出，长正文、长来源和多图区域可独立滚动
79. 用完整键盘复测：发送菜单 Enter/Space 打开、↑↓/Home/End 移动、Escape 返回触发器；预检初始 Tab/Shift+Tab 不逃到背后面板，Escape 保留选择，Cmd+Enter 只发一次，busy 与全部禁用态仍有焦点留在 modal 内
80. 浅色、暗色和系统 Reduce Motion 各检查一次浮层层级、文字/警告对比度、滚动条和开关状态；VoiceOver 依次读出 dialog 标题、目标、回车实际决定、warning/stale、最终正文、取消与确认按钮的可用状态

## Context Firewall 本地文本引擎（Phase 07，RUNTIME-REQUIRED）

81. 在签名 release 包的 WebView DevTools 中调用 `scan_sensitive_text`，输入只含 synthetic 的 PEM、Authorization、JSON API key、数据库 URL、邮箱、电话、身份证、Luhn 卡号、IPv4、Cookie/Session；逐项用 JavaScript `slice(startUtf16, endUtf16)` 验证范围，主界面不得发生变化
82. 调用后检查诊断页与 `toskr-diag.log`：只允许出现 UTF-16 长度、finding 数、类别计数和耗时；搜索 synthetic 原值、完整 Prompt、Token 与可逆 hash 均不得命中
83. 分别提交空文本、含 emoji/组合字符/U+FFFD 的文本、1 MiB 混合文本与超过 2 MiB 的文本：前三者完整返回且界面不阻塞，超限结果必须为 `complete=false + inputTooLong`，不得把未扫描内容视为安全

## Context Firewall 脱敏预览与出站门禁（Phase 08，RUNTIME-REQUIRED）

84. 在真实签名包中发送只含 synthetic 的邮箱、手机号与 fake API key 文本：快速发送必须先本地扫描并自动打开预检；Firewall 区域显示类别、严重级别、数量与遮罩预览，正文原值不得出现在 HUD 或诊断
85. 在同一 Draft 放入两处相同邮箱、emoji/组合字符和交叠 synthetic fixture：逐项、同类、全部替换均保持 Unicode 完整；同值稳定复用 `[EMAIL_01]`，正文已有该编号时从下一号开始
86. 分别用 `requireRedaction / confirmRaw / allowRaw` 方案发送 warn 与 block：逐项替换或明确保留前按钮状态符合策略；confirmRaw 的提示项需一次确认，allowRaw 的 block 必须二次确认，普通点击不能绕过
87. 用单条无 finding 文本和带 finding 文本各走 smart/off/显式预检：无 finding 保持原快速发送；任何 finding、扫描失败或超限都进入预检且不提前调用 Native；快速双击仍只有一个在途意图
88. 确认保留原文后依次修改正文、Prompt、格式、目标 token 与发送方案：旧确认立即失效并重新扫描或要求重开预检；迟到扫描结果不得覆盖新正文
89. 设置 → 目标与发送方案检查 Firewall 默认开启；逐个关闭/恢复邮箱、电话、身份证、银行卡、IP 提示，block 类别没有单独关闭入口；界面明确说明可能误报和漏报
90. 对仍保留 block 原文的三种策略复测“自动按回车”方案：允许发送的明确保留/二次确认路径也必须只粘贴、不按回车；目标漂移与 Native gate 继续优先阻断
91. 完成发送、取消预检、切换数据目录并重启后，业务 JSON、完整备份、活动目录和诊断日志均不含 redaction map 或 synthetic 原值；v9 数据自动迁到 v10 且原 Note/Task/Profile 不丢失

## AI 密钥与传输安全（Phase 09，RUNTIME-REQUIRED）

92. 在设置窗保存 synthetic API key：输入框保存后立即清空，只显示“已配置”和可选更新时间；关闭/重开设置与重启 App 状态仍一致，主窗口和设置窗口的状态变化不闪回
93. 先保存 A，再输入 B 覆盖，使用本地 stub 验证请求只接受 B；点击删除并确认后状态变“未配置”，获取模型、测试连接和现有任务 AI 均给准确提示，不能读取或恢复 A/B 明文
94. 用 v10 临时数据副本放入 synthetic `aiApiKey` 后启动：Keychain 写入成功才从 JSON 清除；故障注入让写入失败时 HUD 明确警告，JSON 中唯一恢复副本仍在且完整备份继续拒绝包含它
95. 分别填写 HTTPS、`http://localhost`、`http://127.0.0.1`、`http://[::1]`、远端 HTTP、userinfo、FTP 和无 host 地址：HTTPS 与三种 loopback 通过策略，其他在发请求前阻断；redirect 不得把 Bearer header 带到第二跳
96. 让本地 stub 延迟响应，期间用 `ps` 检查 Toskr 及子进程：不得出现 curl 子进程、Authorization header、API key、正文或完整 Prompt；诊断、错误 HUD、业务 JSON和完整备份同样搜索不到 synthetic key
97. 用本地 stub 依次走自然语言建任务、拆解子任务、笔记转任务、AI 起标题、获取模型和测试连接；成功行为保持兼容，未配置、删除 key、网络失败和解析失败时原输入/原卡片不得丢失
98. 在浅色/暗色与 380pt 窄设置窗检查 Keychain 状态、覆盖、删除确认、HTTPS 提示和错误文案；Tab/VoiceOver 顺序可达，密钥正文任何时候都不被朗读或显示

## AI 显式转换预览（Phase 10）——已按用户反馈移除（2026-08-12）

99-106.（作废）预检内的「AI 转换预览」面板已整体移除：预检「内容」分区不得再出现 AI 显式转换入口、生成预览按钮或 provider/model 展示；底层 transform 会话机制保留但无 UI 触发点。回归时只需确认：AI 已配置的前提下打开预检，页面不出现「AI 转换预览 / 生成预览」，正文编辑、隐私检查与发送不受影响

## 最近发送与安全恢复（Phase 11，RUNTIME-REQUIRED）

107. 在一次性 TextEdit 文档依次完成 sent、发送前 blocked、Native failed：Target Lens 的历史入口出现对应记录；卡片首屏只显示目标、真实发送时间、内容数量及“发送内容 → 回复”关系，方案、原因、隐私、剪贴板、关联/检查时间和历史动作只在“更多信息”展开后显示，活动账本仍不保存正文或 Prompt
108. 用只含 synthetic secret 的来源完成预检与发送，再检查当前数据目录的 `toskr-delivery-activity.jsonl` / `.1.jsonl`：字段严格白名单，搜索正文、Prompt、API key、target token、redaction map 与 finding preview 均不命中；向副本插入坏 JSON/未知字段后仍可读取其余合法行
109. 用临时数据目录生成超过 500 条、超过 30 天及超过单文件预算的事件：读取结果保持最新 500 条且过期记录消失，主文件与单一归档均有界；切换/迁移/回滚数据目录时历史跟随当前数据集且不串目录
110. 对失败记录先修改原 Note/Task，再点“重新准备”：必须读取当前来源、刷新当前目标、重新跑 Firewall 并打开新预检；历史正文、旧 target token 和旧确认不得复用，确认前 Native 调用数为 0
111. 删除全部来源、只删除部分来源各复测一次：记录显示“来源已不存在/部分来源已不存在”，“重新准备”禁用并给可操作说明；不得猜测正文或只发送残余子集
112. 让原目标 App A 退出、切到 App B 后从 A 的失败记录重新准备：Draft 绑定 B 的新 identity/token 和当前方案，先展示预检且不自动粘贴；只有本次明确确认才允许发送
113. 分别从历史抽屉与设置清除活动记录：二次确认明确“不影响卡片”；清理前后对业务 JSON、任务/分组与媒体做 hash/数量比较，必须完全一致，两个活动文件消失且重开为空
114. 在 380pt 竖面板、260–420pt 上/下横栏、浅色/暗色/Reduce Motion 中打开长历史：宽面板用横向“发送内容 → 回复”，330pt 以下改为上下两步，均不横向溢出且列表内部可滚动；Tab/Shift+Tab 不逃逸，Escape 关闭并返回历史按钮，VoiceOver 先读主关系、展开后再读隐私与诊断信息

## 结果回收与来源关联（Phase 12，RUNTIME-REQUIRED）

115. 向 ChatGPT/Claude 各成功发送一次，再于 30 分钟内从对应应用捕获 synthetic 回答：只有 bundle 精确一致且仅一条成功候选时，捕获 HUD 可提示“可保存为回复”；卡片与活动记录在用户确认前均不得自动出现对应关系
116. 构造同 bundle 的两条成功发送、无成功发送、超出 30 分钟和 bundle 不同四种情况：右键“保存为某次回复”分别显示按时间倒序的候选、明确空态或排除项；弹窗显示当前本机发送内容/回复预览与目标时间，但单候选、多候选都不得默认选中，“保存回复”在用户明确选择前保持禁用
117. 保存后结果 Note 显示“来自发送结果”标记；点击“查看发送内容”可打开仍存在的首个原始 Note/Task，结果卡本身仍可编辑、勾选并再次进入普通 DeliveryDraft；不得出现 Reply 主 tab 或特殊发送旁路
118. 从最近发送抽屉点“尚未保存回复”，候选只含发送之后 30 分钟内同 bundle 的 Note；确认后关系线显示“回复已保存”，发送内容和回复均可独立打开，活动 JSONL 的 `resultCaptured` 仍只新增 result Note ID，不写入弹窗展示的本机预览
119. 将同一回复卡改绑到另一条发送：必须二次确认，旧发送显示“曾保存，现已更换”；再点“这不是对应回复”，回复正文、图片、分组和完成状态保持不变，重新打开/重启后 provenance 确实移除
120. 保存回复后分别删除全部/部分源卡和回复卡：关系线准确显示原内容部分/全部缺失或“回复卡已删除”；不得用历史元数据重建任何正文，也不能把派生检查报告误识别为原回复
121. 用含 `[EMAIL_01]` 的 synthetic 回答复测：同一应用会话且映射仍在时，只有确认敏感内容警告后才显示临时恢复预览；关闭即清除且不写回 Note/活动/备份，换数据目录或重启后明确显示映射已失效
122. 用 v11 临时数据升级 v12，比较旧 Note ID/正文/顺序/媒体 hash 不变；再复测目录切换、完整备份、380pt 竖面板、300pt 窄面板、260–420pt 横栏、浅/暗色、Reduce Motion、Tab/Shift+Tab/Escape/焦点归还与 VoiceOver；检查事件发生在发送之后时，列表排序和“发送于”仍必须使用真实发送时间

## 结果核验（Phase 13，RUNTIME-REQUIRED）

123. 对一张已关联结果卡分别准备完整 JSON、非法 JSON、空结果、短结果、奇数代码围栏和省略号结尾：本地检查自动运行且不联网；格式、必填字段、截断与长度状态准确，只表述规则证据，不宣称结果正确
124. 在结果里分别删除、重复并新增 synthetic `[EMAIL_01]` / `[PHONE_01]`：当前发送会话仍有计数时准确区分丢失/重复/未知；清会话或重启后明确“映射已失效”，不能把陌生占位符判定为安全
125. 输入 JSON dot-path 与必要标题/段落后复测完整和故意遗漏结果：缺失项进入 `blocked`，问题入口可用；来源 Note/Task 被编辑、删除或结果正文变化后，旧报告立即显示过期且保存、AI 与继续发送全部禁用
126. AI 配置 local stub，点击前确认请求数为 0，界面先显示 provider、model、来源/结果字符范围和本地 finding 替换计数；点击后请求只含已脱敏当前来源、已脱敏结果与固定 schema，不含图片、raw finding 或 redaction map
127. 让 stub 返回合法报告、未知字段、非法 JSON、空响应、超时，并在途重复点击/取消/关闭/切数据目录：严格 guard 只接受白名单 JSON；错误与取消不改 result Note，迟到回执不回写，新请求须等旧 transport 收口
128. 分别点击“保存为笔记”和“问题进入预检”：前者生成普通 Note 并保留同一 delivery provenance，正文不复制来源/结果；后者生成问题 Note 后进入既有 Preflight，仍需重新扫描、确认目标且不会自动发送
129. 检查当前数据目录活动 JSONL：`resultVerified` 只含 result Note ID、核验状态与检查/问题计数，不含报告、来源、结果或 AI Prompt；同一发送多次核验时最近发送只展示时间最新的一次计数，清活动立即关闭旧核验会话
130. 在 380pt 竖面板和 260–420pt 上/下横栏复测浅色、暗色、Reduce Motion、长检查列表、Tab/Shift+Tab/Escape/回焦与 VoiceOver：弹窗无横向溢出，正文只在内部滚动，状态、失效原因、AI 数据范围和禁用动作均可读

## 本地成效度量（Phase 14，RUNTIME-REQUIRED）

131. 设置 → 使用概览：用 synthetic 的 sent/blocked/failed、Firewall、脱敏、剪贴板、结果回收与核验事件核对首屏“发送完成/成功率/已保护敏感内容”，展开“趋势和详细数据”后再核对原因/状态分布、重试、`Draft→sendSent` 与 `sendSent→resultCaptured` 中位耗时；0/1/小于 5 个样本时不得给趋势结论
132. 在系统时区跨 0 点生成边界事件，切换近 7 天、近 30 天、全部保留期及 Target Profile/TransformRecipe：本地日历范围和组合过滤准确，图表无正文、Prompt 或结果文本
133. 关闭成效度量后新发送仍成功，恢复账本事件标记为不参与指标，使用概览不再统计这些发送；重新开启后仅新发送进入统计
134. 未配置人工基线时界面不显示节省时间值；分别给 Profile 与 recipe 填传统流程分钟数后，recipe 基线优先，实测流程耗时与“估算累计节省”分开显示，零值/负值/异常大值被拒绝
135. 开始问题计时 → 关联一条成功发送 → 回收并关联结果 → 同步结果关联 → 手动标记解决：只保存 session/delivery/result ID 与时间，start→solved 实测中位数准确；取消路径不进入解决耗时
136. 点击“清除成效历史”后统计归零、计时清空，但人工基线和最近发送恢复记录仍在；卡片、任务、分组与附件内容/数量不变，活动 JSONL before/after hash 不变，业务 JSON 只允许成效代次和问题会话变化；新发送只进入新的 metrics epoch
137. 将保留期从 90 缩到 30、7 天：下次读取压实两个活动文件，过期事件消失且最近发送仍可恢复；再从最近发送抽屉显式清活动，只有活动文件消失，业务数据不变
138. 在最窄设置窗口、浅色/暗色、Reduce Motion、200+ 日样本和长方案名下检查：首次进入只展示本机统计状态与三项摘要，“趋势和详细数据/高级工具/数据与隐私”默认折叠；读取失败显示可重试说明而非一屏零值，首次无记录给出下一步；展开后筛选/卡片/轻量柱图无横向溢出或粗滚动条，Tab/Shift+Tab、Enter/Space 与 VoiceOver 可读开关、范围、样本量、估算标签、计时和清除确认

## 首次真实演练与发布硬化（Phase 15，RUNTIME-REQUIRED）

139. 用全新临时数据目录启动签名包：辅助功能拒绝、已授权但 tap 未建立、等待首个事件、输入监控拦截和完全就绪分别显示不同状态与行动；停留系统设置多久都不显示倒计时或失败结论
140. 复制演练示例到 TextEdit 临时文档，选中全文并双击 ⇧：必须由真实捕获生成包含 `demo.user@example.com` 的普通 Note，再主动切到安全目标、重新识别并在 Target Lens 确认；未就绪目标没有确认入口
141. 打开演练预检：假邮箱必须由真实 Context Firewall 检出，应用替换后 finalText 可核对；无论目标 Profile、AI 改写或预检交互如何变化，界面与 Native 实际请求都保持“只粘贴、不按回车、保留面板”；完成真实安全发送或点“稍后在真实应用演练”均能结束
142. 分别在捕获、目标和预检步骤暂停/退出/重启：重新打开后从原步骤和原示例 Note 继续；用 v13 已完成数据升级后不得自动弹引导，设置 → 关于 → 安全发送演练可主动重跑且不清旧卡片
143. 只用键盘依次完成：展开 Target Lens、打开 Preflight、定位/替换 finding、取消或安全粘贴、打开最近发送、关联结果、打开成效设置；Tab/Shift+Tab/Escape/⌘Enter 不得穿透模态或触发两次发送，关闭后焦点回到稳定触发器
144. 在 380pt 竖面板、260–420pt 上/下横栏、最窄设置窗、浅色/暗色和 Reduce Motion 下复测演练、Lens、Preflight/Firewall、最近发送、Result Return 与成效设置；无横向溢出或粗滚动条，状态不依赖颜色/动画，VoiceOver 读出名称、状态、禁用原因与结果
145. 在同一发布候选环境记录 1 MiB Firewall、500 条活动记录及 50,000 条历史下 Lens+Preflight 的耗时与硬件/OS/Node/Rust 版本；超限扫描继续 fail-closed，活动仍最多 500 条，界面不得渲染整份历史
146. 发布构建重启后静置面板，记录 Toskr 空闲 CPU 与 wakeups；再按 TextEdit、Safari/Chrome、Claude/ChatGPT、Codex/Cursor、Terminal、Pin、伴随、四缘边栏、全屏 Space 和可用多屏逐项回归。无法取得 TCC、VoiceOver、多屏或 wakeups 证据时必须保留 `RUNTIME-REQUIRED`

## 可选图片 Context Firewall（Phase 16，RUNTIME-REQUIRED）

147. 只用专门制作的 synthetic 截图，分别包含假邮箱、假手机号、假 token 与假账号；打开预检后核对每张图独立显示本地 OCR 状态、类别、严重级别、图片尺寸和区域框，正文、诊断与活动中不得出现 OCR 原文
148. 用横图、竖图、Retina 2x、多行和边缘文字检查区域框：缩放预览或出现 `object-contain` 留白时仍与真实文字重合；旋转/方向元数据不支持或识别不稳时必须明确显示风险，不得把空白框当作安全
149. 分别点击“遮挡此文字区域”“遮挡此图全部”“遮挡全部图片敏感区域”：首版只生成纯色不可逆遮挡；同一 OCR observation 内多个 finding 一并覆盖，未处理 block 在所有 Profile 下都禁用发送
150. 比较遮挡前后原图文件 SHA-256 与预览：原图 hash/内容不变，实际发送列只切换到带独立 hash 的 Draft 临时副本；扫描后用同名不同像素替换原图/副本时，Native 必须在剪贴板与窗口副作用前返回 `image_changed`；恢复发送原图后临时副本被清理，原图仍可正常预览
151. 多图中让第一张识别成功、第二张 OCR 失败、第三张包含 block：三张状态和遮挡互不污染；严格方案阻止失败原图，其他方案必须逐图显式确认且确认绑定当前扫描 revision 与目标 token
152. 在遮挡请求/OCR 进行中取消、关闭、重开同图、切数据目录并模拟应用强退：迟到回执不得写入新 Draft；取消/发送后清理当前副本，重启清理崩溃遗留，任何清理都不得触碰普通媒体原图
153. 向 TextEdit 或一次性测试目标执行安全发送：目标只收到已遮挡副本，图片顺序不变且不自动绕过既有目标/来源/剪贴板门禁；真实接收截图、操作时间线和诊断锚点必须归因到同一签名构建
154. 在 380pt 竖面板和 260–420pt 上/下横栏复测原图/发送图并排、长类别、浅色/暗色、Reduce Motion、Tab/Shift+Tab/Escape/回焦与 VoiceOver：内部细滚动，无水平溢出，状态不只依赖颜色；自动 DOM/SSR 证据不能替代签名原生窗口

## 主页面页签双击回到列表开头（交互优化，RUNTIME-REQUIRED）

155. 在竖向面板分别把剪贴、笔记、任务滚到列表中下部：单击页签仍只切换页面，双击当前或其他页签平滑回到对应列表顶部；卡片焦点、勾选和分组展开状态不变，视图不得被旧焦点再次拉回
156. 在 260–420pt 上/下横栏把三类列表横向滚到中后部，双击对应页签回到最左起点；开启 Reduce Motion 后立即回到起点；短双击不得误触页签拖拽，超过既有阈值的拖拽重排仍正常

## 笔记分组快速新增（交互优化，RUNTIME-REQUIRED）

157. 在展开和折叠的普通笔记分组上分别悬停：`…` 左侧出现小号“添加内容”按钮；点击后目标分组切换准确，折叠组自动展开，焦点落到底部笔记输入框，输入文字/链接/代码或粘贴图片后均进入该分组
158. 只用键盘 Tab 聚焦添加按钮并提交一条内容，确认按钮有明确分组名称、焦点环与原生提示；在 380pt 窄面板、长分组名、浅色/暗色和 Reduce Motion 下不挤压标题/数量，原 `…` 菜单、分组拖拽和双击改名不受影响

## Target Lens 应用名字阶微调（视觉优化，RUNTIME-REQUIRED）

159. 在 260–380pt 面板分别显示短名 ChatGPT 与超长应用名：应用名使用 label 字阶并保留半粗主层级，“可发送/需确认”降为 micro 字阶；图标、分隔点和状态点垂直居中，长名称优先截断且状态始终可见

## 使用概览信息层级优化（体验优化，RUNTIME-REQUIRED）

160. 在签名设置窗口进入“使用概览”，分别制造读取中、读取失败、零记录、筛选为空和有记录状态：读取失败不得渲染零值指标且应说明不影响发送；有记录首屏只显示三项结果摘要，详细指标、传统用时、问题计时、留存与清除均按主题折叠；在 560pt 窄窗、浅色/暗色、Reduce Motion 下用鼠标与键盘展开/收起，内容无裁切、焦点可见、VoiceOver 能读出分组名称与说明

## 预检手动重新检测（安全交互，RUNTIME-REQUIRED）

161. 构造文本与图片均为 0 项的预检，点击 Context Firewall 的“重新检测”：按钮立即变为禁用的“检测中”，确认发送全程保持锁定；文本完成后串行重跑原图 OCR，Native 诊断本次 `cache_hit=false`，完成后恢复结果与按钮。扫描中连点不得重复调用，途中编辑正文/关闭或重开 Draft 时旧回执不得写入；已有图片遮挡时先提示会恢复原图，取消保持旧副本，确认后清理旧 token、推进 Draft revision 并要求重新处理

## 默认贴边收起与快捷键保护（窗口交互，RUNTIME-REQUIRED）

162. 升级含 `autoEdgeHide=false` 的旧数据与全新数据分别启动：主面板停靠菜单和设置 → 通用均不再出现“自动贴边隐藏”开关；首次呼出即使默认位于屏幕右侧也保持完整显示，只有真实拖动到可收起的外侧屏缘、移开鼠标后才滑出，触边能再次唤回
163. 开启伴随磁吸：目标应用窗口有效时保持原有左右吸附、同高和跟随；切到无有效窗口/目标退出时立即解除接管、恢复自由层级且可任意拖动，拖到可收起屏缘仍能滑出；目标重新出现后可再次吸附。关闭磁吸后同样保持自由拖动 + 手动落边收起
164. 分别用专用面板快捷键与双击触发呼出自由位置、已入坞和贴边隐藏的面板：不拖动时切到其他应用、移开鼠标均不得收起；真实拖动后恢复普通失焦规则，拖到屏缘后恢复光标收起；按 Esc 时已入坞面板沿屏缘滑出并归还焦点，未入坞面板按原逻辑关闭。托盘/气泡打开不应误进入快捷键保护；在多屏接缝、浅色/暗色、Pin、Reduce Motion 下复测无误入坞或唤回失败

## 可逆化名词典（隐私能力，RUNTIME-REQUIRED）

165. 设置 → 目标与发送方案 →「可逆化名」：用预置类别（用户/商户/订单/项目）与一个自定义类别（如 VENDOR/供应商）各建一条词条：占位符按类别独立编号（[USER_01]、[VENDOR_01]）；删除某条后再新建同类别词条，编号不复用已删除的号；自定义类别码填 EMAIL 等保留字或小写字母时被拦截并给出原因
166. 开启可逆化名后发送含词典原文的正文：快速发送与显式预检两条路径都自动替换（不遗漏）；词典同时含「张三」与「张三丰」时长词优先命中，不出现嵌套/错位替换；预检「可逆化名」分区逐处展示「原文 → 占位符」且显示原文（词条为用户自录，非脱敏对象）
167. 正文字面已含 `[USER_01]` 时发送：既有占位符不被二次处理；本地预演区输入示例句实时显示化名结果且不触碰剪贴板
168. 预检里对某处命中点击「还原为原文」：该处立即变回原文、分区计数收缩，并触发隐私正则重扫描（如原文是邮箱则重新出现隐私命中）；修改正文或方案导致预检重建后，词典命中状态随之刷新
169. 关闭「启用可逆化名」总开关后发送同一段正文：不再替换，预检不显示「可逆化名」分区，设置卡内词典/预演/自动恢复区随开关折叠
170. 对带占位符的旧卡片（混合词典占位符与 [EMAIL_01] 等隐私占位符）：卡面 footer 显示「含 N 处化名 · 点击恢复」（有 provenance 徽标时走悬浮「恢复化名」按钮）；点击后仅词典占位符还原、隐私占位符原样保留，HUD 可撤销且撤销后完全复原
171. 开启「捕获时自动恢复化名」后划词捕获一段含已知占位符的文本：新卡片入库即为已还原原文，捕获 HUD 预览为还原后内容并追加「已恢复 N 处化名」一行，卡面不再出现恢复徽标；关闭该开关后同样捕获，卡片保留占位符并显示徽标
172. 重启应用后（不依赖任何发送会话）对旧卡片手动恢复：词典恢复不依赖 deliveryId 与内存映射，重启前后行为一致；活动记录（最近发送抽屉「更多信息」的隐私保护替换数）计入词典替换数，但正文与映射不落活动日志
173. 词典有词条时做一次完整备份并在新数据目录恢复：词条（含原文明文）随设置一并保留；设置卡明示「词典原文以明文随本地数据文件保存，并包含在完整备份中」

## 发送设置页分区与术语统一（体验优化，RUNTIME-REQUIRED）

174. （2026-08-12 改版）「目标与发送方案」页：当前目标卡常驻顶部，「发送方案管理」「隐私与化名」「提示词组」默认折叠，一屏内看完；点当前目标卡「编辑此方案」→ 管理区自动展开、选中解析到的方案并平滑滚动（只滚 main 容器，不得露白底）；从 HUD 深链到指定发送方案同样自动展开选中；分组折叠展开状态互不干扰
175. 对比全局「发送前隐私检查」卡与方案编辑器分区⑤「隐私命中后处理策略」：两处标题不再字面撞车，能分辨「总开关」与「命中后处置粒度」；预检、AI 面板、演练文案中不再出现裸「Context Firewall」术语（设置卡标题保留括注）
176. 在 DeliveryTrack 预演轨道与「当前匹配」卡触发同一匹配来源（临时选择/重复绑定冲突/应用指定/默认方案）：两处根词一致（仅本次手动选择/重复绑定冲突/应用指定/未识别应用的默认方案）
177. hover「测试当前目标」出现「仅本地重新计算…不访问剪贴板」提示；刷新按钮 aria/title 为「刷新（重新识别系统前台应用）」——点击前即可区分本地演算与真实系统调用

## 面板目标条提示补全（可发现性，RUNTIME-REQUIRED）

178. hover 目标条右侧 History 图标出现原生提示「打开最近发送」；存在隐藏风险（隐私检查已关闭/自动回车已开启/方案存在重复绑定冲突任意组合）时 hover 展开箭头显示「展开发送详情 · 具体原因」，警示圆点因此可解释；无风险时仅显示「展开发送详情」，VoiceOver 读出的 aria-label 与 title 一致

## 预检敏感项操作区与底栏解释（体验优化，RUNTIME-REQUIRED）

179. 构造一条含假邮箱的预检：命中卡内「替换为占位符」为实底主操作、「同类全部替换」为描边按钮、「保留原文发送」右置弱化且 hover 有后果说明，定位为卡头右侧图标钮——四者不得再呈现选项卡观感；单条命中不出现「一键全部替换」，两条以上才出现
180. 逐项处理完全部命中后：分类计数变为「无待处理项」，出现绿色「已替换 N 处敏感内容，本项检查通过」（或全部确认保留时「敏感项已全部处理，可以发送」）；发送按钮同步解锁
181. 让发送目标失效（预检开着时切走目标应用再回 Toskr）：底栏完整显示「发送目标已变化，请重新确认目标 · 切回 <应用名> 后这里会自动恢复」不被截断，hover 禁用的发送按钮出现同一原因 tooltip；**切回目标应用后警告自动消失、按钮解锁（recoverOpenPreflightTarget 同身份换令牌）且已处理的隐私/化名状态保留**；把目标应用彻底退出重开后再回 Toskr：提示改为「目标应用已重启或窗口已变化，请取消后重新发起发送」，不得自动重绑；另验证空内容/回车未确认两种禁用原因的状态行文案

## 恢复闭环可见化与最近发送简化（体验优化，RUNTIME-REQUIRED）

182. 设置 → 可逆化名 → 本地预演输入含词典原文的句子：依次显示「① 发出前·已自动化名」与「② 捕获回复时·本机自动恢复」两段，恢复段与原文一致时出现绿色「可逆闭环验证通过」；全程不触碰剪贴板不发送
183. 完整闭环真机走查：词典录入实体 → 发送含该实体的卡片（预检「可逆化名」分区可见替换）→ 在目标 AI 应用划词捕获含占位符的回复（HUD 出现「已恢复 N 处化名」行且入库即原文）；关闭「捕获时自动恢复」重试：回复卡带「含 N 处化名」徽标，最近发送抽屉对应记录出现化名条，点「恢复为原文」后条目消失、卡片正文还原、HUD 可撤销
184. 最近发送抽屉：已发送记录绿勾；预检未完成/发送中记录用中性时钟图标 + 灰色状态词（不得出现绿勾+未完成的矛盾组合）；未发送且无回复的记录只显示单行「发送内容」入口，不再渲染灰色「发送未完成」回复占位卡；失败记录仍可「重新准备」
185. 学习路径「本地恢复」演示完成后出现指向「设置 → 目标与发送方案 → 可逆化名」的真实词典指引；预检图片区标题为「图片隐私检查」，全界面（除设置卡括注外）不再出现裸「Firewall/Context Firewall」英文术语


## 回合视角自动归位与两页极简（第一性重构，RUNTIME-REQUIRED）

186. 发送一条含词典实体的卡片到 AI 应用，划词捕获含占位符的回复：HUD 依次出现预览、「已恢复 N 处化名」「已自动保存为最近发送的回复」三类信息；打开最近发送，该记录已是「已收到回复」且提供「打开回复」——全程零手动配对。撤销捕获后记录回到等待态（更多信息显示回复卡已删除）
187. 无占位符的普通捕获（仅同目标+时间窗证据）：仍走「可关联…请右键确认」建议流程，不得自动写入；多候选时无建议也无自动归位；重启应用后（会话映射清空）同样退回建议流程
188. 最近发送回合列表：每行凭出站摘要一句可辨认（来源卡删除后退回计数）；等待回复行显示「下一步：在 AI 应用划词回复（双击 ⇧）即可自动带回 · 手动选择」；已收到行有「打开回复」与（含化名时）「恢复化名」；失败行「重新准备」；时间/隐私计数/核验/更换/取消对应全部收在「更多信息」内；准备中/预检未完成/发送中折叠为底部「未发出的记录 N 条」，展开后单行可点查看
189. 使用概览：页顶为学习路径（完成态单行）+ 时间范围 + 三指标；「本机使用统计」开关在「数据与隐私」折叠区首行且可正常开关；零记录空态出现「发送第一条内容试试：双击 ⇧ 捕获，勾选后 ⌘⏎ 发送」；把筛选调到无数据后出现「清除筛选」，点击后回到全部保留期无筛选

## 化名词典快捷入口（体验优化，RUNTIME-REQUIRED）

190. 预检出现邮箱/手机号等提示级命中时，命中行有「加入词典」菜单（高风险 block 项不显示）：选类别后词条持久入典；正文未手工编辑时草稿自动重建——该值变为词典占位符、隐私命中随之消失；已手工编辑则提示用「重新检测」应用
191. 详情窗 footer 出现「选词模式」开关（代码/链接卡除外）：开启后正文按词分块，点选一个词高亮，再点另一词扩展连选范围；选中工具条出现「加入化名词典」，面板内原文预填可修改、类别按内容自动建议（邮箱/电话→联系方式、编号→订单、默认用户）可切换，确认后 HUD 回执「已加入化名词典：X → [Y_NN]」；再次发送含该原文的内容自动替换
192. 只读的「合并发送内容」预览窗与普通拖选（不开选词模式）同样可用「加入词典」；只读来源不出现粗体/链接等改写控件；词典总开关关闭时加入仍成功但 HUD 明示「总开关当前关闭」

## 笔记编辑自动保存（防写一半白写）

193. 详情窗编辑正文超过 2s 后强杀应用（`pkill -9 -x toskr`）重开：改动已在（最多丢最后 2s 内的输入）；正常路径 ⌘⏎ 保存出可撤销「已保存」，悬停「撤销」后回到本次编辑前
194. 详情窗编辑中途分别按 Esc、点 ⊗ 关窗、在主面板打开另一张卡：三条路径内容都保留并出可撤销「已保存」；把草稿全部清空再 Esc → 还原编辑前原文、无警告气泡
195. 面板内预览层（Space→编辑）与图片备注编辑同语义：改动后 Esc/关闭不丢内容、收尾气泡可撤销；图片备注清空后退出 = 清除备注（原有语义）

## 本地图片入卡（复制 / 拖拽 / 粘贴）

196. Finder 选中 1 张本地图片 ⌘C：剪贴板历史收进一张图片卡（非文件名文本卡）；多选 3 张 ⌘C 收 3 张图片卡；重复复制同一张 → 像素哈希去重不重复入库；复制 .heic/.pdf 维持旧行为（不成图片卡）
197. 输入框（DraftInput）⌘V 粘贴 Finder 复制的多张图片文件：全部进入待发 chip；确认后成多图卡
198. 文本详情窗（普通文字/合并卡）：查看态从 Finder 拖入图片 → 出现「松开以添加图片」提示 → 松开后图片直接落库为尾部附件，HUD 出可撤销「已保存」，撤销后图片消失；编辑态拖入 → 进草稿缩略条，Esc 清空放弃时不留孤儿；查看态 ⌘V 同拖拽语义
199. 有序图文卡详情：拖入/粘贴图片追加为末尾图片块（图片锚点位置不变）；编辑态拖入随自动保存持久化
200. 图片卡原尺寸预览窗：拖入/查看态 ⌘V 追加为多图（←/→ 可翻到新图），HUD 可撤销；重复图片提示已存在；纯看图（无笔记上下文）不响应拖入
201. 首次导入 桌面/文稿 下的图片时系统弹目录访问授权：允许后导入成功；拒绝后 HUD 提示导入失败且应用不崩溃

## 详情窗体验批次（2026-08-16：字号/MD 编辑/标签/撤销/工具条/核对清单）

202. 任务行优先级色条与右键菜单四档选项均可点击生效；行拖拽与 Tab 聚焦把手拖拽不受影响（笔记舒适卡把手同验）
203. 详情窗 ⌘+/⌘-/⌘0 调字号即时生效且重开保留；设置 → 外观「详情窗字号」滑杆与窗内快捷键双向同步（窗开着拖滑杆实时变化）
204. 图片预览滚轮缩小到 0.2×（居中、拖拽仍拖窗）、放大到 8×；缩小态双击复位适配
205. 详情窗编辑态点「渲染」看草稿的 Markdown 效果、核对清单可点选；「原文」返回后光标/撤销历史仍在；查看态 Markdown 渲染里点选核对清单方框即改原文（静默，无气泡轰炸）
206. 详情窗标签行：＋标签 内联输入回车连续添加、# 前缀自动剥、重复不添加、上限 8 个；chips 悬停 ⊗ 摘除；改动即持久化（主面板卡片同步）
207. 详情窗查看态 ⌘Z：撤销最近可撤销动作（编辑保存/图片添加），窗内内容同步回退；无可撤销时提示
208. 编辑态有「编辑中」徽标与正文区蓝描边，与查看态一眼可辨
209. 选中工具条出现在选区上方（顶部空间不足转下方），滚动跟随；编辑态 textarea 选中同样就近；选词模式回退底部居中。「文本」菜单展开后换选区/用过一次仍保持展开，Esc 或点空白后收起
210. 格式菜单「核对清单」⌥⌘6（代码块 ⌥⌘7）：选中行转 - [ ]，渲染为方框，勾选=主色底白勾
211. 全新环境首启（或清 webview localStorage 后）：应用启动即显示主面板；既有用户升级不改伴随/图钉偏好
212. 选中工具条贴近窗顶时打开「文本」菜单：向下展开完整可见不被裁剪；链接/词典面板同规则
213. 目标为 Chrome 时短暂唤起无身份系统浮层（如截图工具/权限面板）再回 Toskr 发送：30s 内目标保持「可发送」直接发出；持续停留超 30s 才降级「目标已失效」；切回 Chrome 状态即时恢复且不弹方案确认
